extends layout



block content
    nav.navbar.navbar-light.bg-light
        .container
            button#cart.btn.btn-primary Cart 
                span#badge.badge.bg-danger 0

            

    //- .glide.products
    //-     .glide__track(data-glide-el="track")
    //-         ul.glide__slides
    //-             each item in items
    //-                 li.glide__slide
    //-                     .card.product-item
    //-                         img.card-img-top(src=`${item.image}` alt='')
    //-                         .card-body
    //-                             h5.card-title= item.name
    //-                             p.card-text
    //-                             | Price: #{item.price}
    //-                             div
    //-                                 button(data-id=`${item.id}`).btn.btn-primary Add to cart
    //-     .glide__bullets(data-glide-el="controls[nav]")
    //-         - var count = 0
    //-         each item in items
    //-             .glide__bullet(data-glide-dir=`=${count}`)
    //-             - count++
    .container
        .row
            .col.text-center.mt-3.mb-3
                .btn-group
                    button.btn.btn-danger(type='button', data-size='s') S
                    button.btn.btn-warning(type='button', data-size='m') M
                    button.btn.btn-success(type='button', data-size='l') L

        .row.products
            each item in items
                .col.col-4.product(data-size=`${item.size}`)
                    .card.product-item
                        img.card-img-top(src=`${item.image}` alt='')
                        .card-body
                            h5.card-title= item.name
                            p.card-text
                            | Price: #{item.price}
                            div
                                button(data-id=`${item.id}`).btn.btn-primary Add to cart